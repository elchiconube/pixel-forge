import React from 'react';
import Game from './game/Game';

// Este componente es un wrapper que utiliza la nueva arquitectura modular
const PixelForgeGame: React.FC = () => {
  return <Game />;
};

export default PixelForgeGame;
